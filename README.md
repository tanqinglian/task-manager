# 桌面任务管理器

基于 Tauri + React + Zustand + Tailwind CSS 构建的桌面任务管理应用。

## 功能特性

### 1. 任务管理
- ✅ 任务增删改查
- ✅ 维度分类（周、月、季度、半年、年度）
- ✅ 任务状态管理（规划中、实现中、已完成）
- ✅ 任务筛选和统计
- ✅ 标签支持
- ✅ 富文本描述编辑

### 2. 文本编辑器
- ✅ 基于 Tiptap 的富文本编辑器
- ✅ 支持粗体、斜体、删除线
- ✅ 支持标题、列表、引用、代码
- ✅ 本地文件保存

### 3. 定时提醒
- ✅ 每天上午 9:20 和下午 6:00 提醒
- ✅ 显示需要更新的任务列表
- ✅ 系统原生通知

### 4. 数据持久化
- ✅ LocalStorage 本地存储
- ✅ 可选 SQLite 数据库

## 技术栈

| 技术 | 用途 |
|------|------|
| Tauri 2 | 桌面应用框架 |
| React 19 | 前端框架 |
| TypeScript | 类型安全 |
| Zustand | 状态管理 |
| Tailwind CSS 4 | 样式框架 |
| Tiptap | 富文本编辑器 |
| date-fns | 日期处理 |

## 项目结构

```
task-manager/
├── src/                          # 前端源码
│   ├── components/               # React 组件
│   │   ├── DimensionSidebar/    # 维度侧边栏
│   │   ├── TaskList/            # 任务列表
│   │   └── TaskEditor/          # 任务编辑器
│   ├── store/                   # Zustand 状态管理
│   │   └── taskStore.ts         # 任务 Store
│   ├── hooks/                   # 自定义 Hooks
│   │   └── useNotificationScheduler.ts
│   ├── types/                   # TypeScript 类型
│   │   └── index.ts
│   ├── constants/               # 常量配置
│   │   └── index.ts
│   ├── App.tsx                  # 主应用
│   ├── main.tsx                 # 入口
│   └── index.css                # 全局样式
├── src-tauri/                   # Tauri 后端 (Rust)
│   ├── src/
│   │   ├── lib.rs              # Tauri 命令
│   │   └── main.rs
│   └── Cargo.toml
└── package.json
```

## 开发指南

### 安装依赖

```bash
pnpm install
```

### 前端开发

```bash
pnpm run dev
```

### Tauri 开发（需要 Rust 环境）

```bash
pnpm run tauri dev
```

### 构建

```bash
# 前端构建
pnpm run build

# 完整桌面应用构建（需要 Rust）
pnpm run tauri build
```

## Rust 环境安装

如果需要构建完整的桌面应用，请先安装 Rust:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

然后安装 Tauri 所需的系统依赖:
- macOS: `xcode-select --install`
- Windows: [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
- Linux: `sudo apt install libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev`

## 使用说明

### 创建任务
1. 点击右上角 "新建任务" 按钮
2. 填写任务标题和描述
3. 选择状态、维度和截止日期
4. 可选添加标签
5. 点击 "创建任务"

### 编辑任务
1. 点击任务卡片打开编辑对话框
2. 修改相关信息
3. 点击 "保存修改"

### 更新状态
鼠标悬停在任务卡片上，点击状态切换按钮:
- 🟠 规划中
- 🔵 实现中
- 🟢 已完成

### 维度切换
点击左侧维度栏切换不同时间维度的任务。

## 数据存储

任务数据保存在浏览器的 LocalStorage 中，键名为 `task_manager_tasks`。

## 待实现功能

- [ ] SQLite 数据库集成
- [ ] 导出任务为 Markdown
- [ ] 任务搜索功能
- [ ] 拖拽排序
- [ ] 主题切换（深色模式）
- [ ] 快捷键支持

## License

MIT
